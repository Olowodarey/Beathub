# Use a flat, hoisted node_modules (npm-style) instead of pnpm's symlinked
# layout. Vercel's function file-tracing (@vercel/nft) can't follow pnpm's
# symlinks, which otherwise leaves runtime deps like express/music-metadata out
# of the deployed function. Hoisting makes them traceable.
node-linker=hoisted
